# Pipeline configuration (loaded by Pipelines container at runtime)
# Currently unused — Task 1 skeleton has no env-driven config.
# Future tasks will add:
#   REDIS_URL=redis://localhost:6379/0     (Task 5)
#   PII_LOG_LEVEL=INFO                      (Task 9)
